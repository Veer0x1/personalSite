overwrite: true
schema: "https://gql.hashnode.com/"
documents: "lib/graphql/**/*.graphql"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript-common"
  ./graphql.schema.json:
    plugins:
      - "introspection"
